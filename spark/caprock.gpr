--  Projektdatei fuer den SPARK-Portierungsversuch des Cap-Space (Experiment).
--  Lauf:  tools/spark-beweis.sh
project Caprock is
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ();

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat2022", "-gnatwa");
   end Compiler;
end Caprock;
