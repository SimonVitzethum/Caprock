# Synchrone IPC: Endpoints (Rendezvous Sender/Empfänger), Call/Recv/Reply.
# Orchestriert Scheduler-Block/Unblock + Nachrichtentransfer zwischen TrapFrames.
[package]
name = "caprock-ipc"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
caprock-abi = { path = "../caprock-abi" }
caprock-hal = { path = "../caprock-hal" }
caprock-sched = { path = "../caprock-sched" }
