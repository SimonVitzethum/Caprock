# Capability-System: CNode/Slots, Capability-Derivation-Tree (CDT),
# copy/mint/move/delete/revoke. Reine, sichere Datenstruktur-Logik — kein unsafe.
[package]
name = "caprock-cap"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
caprock-mem = { path = "../caprock-mem" }
caprock-slab = { path = "../caprock-slab" }
