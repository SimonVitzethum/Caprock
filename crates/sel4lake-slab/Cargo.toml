# Slab: zur Boot-Zeit dimensionierte Tabellen (statt `.bss`-Arrays fester Größe).
# Genau EINE `unsafe`-Domäne (Rohspeicher -> typisierte Tabelle), danach nur noch
# bounds-geprüfte Zugriffe.
[package]
name = "sel4lake-slab"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
