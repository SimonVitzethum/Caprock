[package]
name = "caprock-virtio"
version = "0.1.0"
edition = "2021"
# **GRENZ-CRATE: permissiv, und das ist tragend** (docs/grenze.md). Jede virtio-PD linkt
# Region/Typestate/Transport -- stuende das unter AGPL, waere jede Treiber-PD mit AGPL-Code
# gelinkt und die ABI-Ausnahme gegenstandslos.
license = "MIT OR Apache-2.0"

# **Keine Abhaengigkeiten.** Das ist der Zweck dieser Crate, nicht ein Zufall: sie soll von einer
# Userland-Treiber-PD gelinkt werden koennen (todo A-5.1), und alles, was sie an den Kernel
# bindet, waere genau die Bindung, die dort weg soll.
[dependencies]
